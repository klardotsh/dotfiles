# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe -march=native -mtune=native"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
LDFLAGS="-Wl,-O2 -Wl,--as-needed"

VIDEO_CARDS="intel i965"
CPU_FLAGS_X86="aes avx avx2 f16c fma3 mmx mmxext pclmul popcnt sse sse2 sse3 sse4_1 sse4_2 ssse3"

MAKEOPTS="-j9"
PORTAGE_NICENESS="19"
INPUT_DEVICES="libinput"

EMERGE_DEFAULT_OPTS="--jobs=9 --load-average=9"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

USE="-systemd -networkmanager -consolekit dbus elogind alsa jack X wayland lm-sensors vulkan ffmpeg python unicode evdev libinput policykit zsh-completion pulseaudio gles opengl kms libsamplerate ao realtime libressl threads opus theora vorbis logrotate truetype vdpau vaapi pcsc-lite"
CURL_SSL="libressl"

ACCEPT_LICENSE="@BINARY-REDISTRIBUTABLE @FSF-APPROVED-OTHER"
L10N="en"
GRUB_PLATFORMS="efi-64"

PYTHON_TARGETS="${PYTHON_TARGETS} python3_8"
RUBY_TARGETS="ruby25"

GENTOO_MIRRORS="https://gentoo.osuosl.org/"
