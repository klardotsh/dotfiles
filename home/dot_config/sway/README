`config.tmpl` includes `woods.config` on the `woods` host.

Retired host files stay in the source for history and remain ignored by chezmoi.

Use `chezmoi execute-template` to inspect the rendered `config` before applying it.
