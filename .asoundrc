pcm.!default {
	type plug
	slave.pcm plugequal;
}

ctl.!default {
	type hw
	card 1
}

ctl.equal { 
	type equal; 
} 
pcm.plugequal { 
	type equal; 
	slave.pcm "plug:dmixer"; 
}
pcm.dmixer {
	type dmix
	ipc_key 1024
	slave {
		pcm {
			type hw
			card PCH 
			device 0
		}
		buffer_size 4096
	}
}

