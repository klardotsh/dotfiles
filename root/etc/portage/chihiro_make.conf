WARNING_FLAGS="-Werror=odr -Werror=strict-aliasing"

COMMON_FLAGS="-O3 -pipe -flto=thin -march=native -mtune=native ${WARNING_FLAGS}"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
RUSTFLAGS="${RUSTFLAGS} -C opt-level=3 -C target-cpu=native -Clinker-plugin-lto"

CGO_CFLAGS="${CFLAGS}"
CGO_CXXFLAGS="${CXXFLAGS}"
CGO_FFLAGS="${FFLAGS}"
CGO_LDFLAGS="${LDFLAGS}"

GOAMD64="v3"

FONT_TYPES="otf"

#FEATURES="ccache"
MAKEOPTS="-j9"
EMERGE_DEFAULT_OPTS="--jobs 7 --load-average 7.5"
PORTAGE_SCHEDULING_POLICY="idle"

ACCEPT_LICENSE="@BINARY-REDISTRIBUTABLE @EULA Bitwig intel-ipu6-camera-bins"

CC="clang"
CPP="clang-cpp"
CXX="clang++"
AR="llvm-ar"
NM="llvm-nm"
RANLIB="llvm-ranlib"

# NOTE: This stage was built with the bindist USE flag enabled

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C.UTF-8

GENTOO_MIRRORS="https://gentoo.osuosl.org/"
