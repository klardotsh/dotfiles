# udev rule file for Keychron mice for use with Keychron Launcher
#
# Place in /etc/udev/rules.d/ and reload udev with
# doas/sudo udevadm control --reload-rules && doas/sudo udevadm trigger
#
# (Use lsusb to see your devices while the mouse is plugged in)

SUBSYSTEMS=="usb", ATTRS{idVendor}=="3434", GROUP="input", MODE="0666", SYMLINK+="keychron_usb"
KERNEL=="hidraw*", ATTRS{idVendor}=="3434", GROUP="input", MODE="0666", SYMLINK+="keychron_hidraw"
